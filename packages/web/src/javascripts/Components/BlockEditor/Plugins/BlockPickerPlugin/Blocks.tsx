import { BlockPickerOption } from './BlockPickerOption'
import { LexicalEditor, $createParagraphNode, $getSelection, $isRangeSelection, FORMAT_ELEMENT_COMMAND } from 'lexical'
import { $wrapNodes } from '@lexical/selection'

export function GetBlockPickerOptions(editor: LexicalEditor): BlockPickerOption[] {

}
